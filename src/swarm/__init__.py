"""Swarm: a locally runnable multi-agent research and problem-solving framework.

Dynamic functional roles, a deterministic controller that owns every transition,
independent criticism and validation, host-verified evidence as the only route into
validated knowledge, selective cross-pollination, bounded exploration waves, a computed
synthesis gate, an independent final review, and terminal outcomes a run can defend.

Entry points: ``swarm.cli`` (``python -m swarm``), ``swarm.scenario``,
``swarm.runner``, ``swarm.orchestration.WorkController`` and ``swarm.inspection``.
"""

__version__ = "1.0.0"
