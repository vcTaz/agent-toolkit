{
  "$comment": "External executables this setup depends on. CORRECTED 2026-09-20 against a live cloud session: `cloudPreinstalled` now records what `command -v` returned in that session, not what the documentation lists. Earlier revisions of this file were wrong twice in opposite directions - once claiming ripgrep and uv were absent when they are present, once claiming gh is present when it is not.",
  "schemaVersion": 1,
  "verifiedAgainst": {
    "date": "2026-09-20",
    "source": "command -v run in a live Claude Code cloud session",
    "supersedes": {
      "date": "2026-09-19",
      "source": "code.claude.com/docs/en/cloud-environments#installed-tools",
      "note": "The documentation list was taken as the fact. It is right about every entry here except gh."
    }
  },
  "required": {
    "git": {
      "scope": "both",
      "cloudPreinstalled": true
    },
    "jq": {
      "scope": "both",
      "cloudPreinstalled": true,
      "note": "Hard dependency of the rtk PreToolUse hook (local only)."
    },
    "node": {
      "scope": "both",
      "cloudPreinstalled": true,
      "note": "Cloud ships 20, 21 and 22; 22 on PATH. Local observed 24.21.0."
    },
    "python3": {
      "scope": "both",
      "cloudPreinstalled": true,
      "note": "tools/check.py requires 3.9+, stdlib only."
    },
    "rg": {
      "scope": "both",
      "cloudPreinstalled": true
    },
    "gh": {
      "scope": "machine-local",
      "cloudPreinstalled": false,
      "note": "MEASURED 2026-09-20 in a cloud session: `command -v gh` and `command -v hub` both return nothing and nothing gh-shaped is anywhere on PATH, though the official installed-tools list names gh. This file declared it present, and that declaration cost a real round trip when a plan was written around `gh repo create`. Do not write one. In a cloud session use the GitHub MCP tools (mcp__github__*) or plain git, which authenticates through the proxy by credential injection. Locally gh is present and authenticates normally."
    },
    "uv": {
      "scope": "both",
      "cloudPreinstalled": true
    }
  },
  "machineLocal": {
    "rtk": {
      "purpose": "Token-reducing CLI proxy; invoked by a PreToolUse Bash hook on every command.",
      "repo": "https://github.com/rtk-ai/rtk",
      "installedVersion": "0.42.1",
      "sha256": "10445d460a1bea8314163bece8b69a336f715823bc225acd28ef6654aa052a31",
      "artifact": "rtk-x86_64-unknown-linux-musl.tar.gz",
      "install": "curl -fsSL https://raw.githubusercontent.com/rtk-ai/rtk/refs/heads/master/install.sh | sh",
      "minimumForHook": "0.23.0",
      "WARNING": "Do NOT run 'cargo install rtk'. The crates.io crate named 'rtk' is a DIFFERENT project (reachingforthejack/rtk, 'Rust Type Kit'). Installing it replaces the working binary with one that has no 'rewrite' subcommand and silently disables the PreToolUse hook on every Bash call."
    },
    "srt": {
      "purpose": "Anthropic Sandbox Runtime. The OS-enforced boundary claude-safe runs Claude Code inside.",
      "package": "@anthropic-ai/sandbox-runtime",
      "installedVersion": "0.0.76",
      "install": "npm install -g @anthropic-ai/sandbox-runtime",
      "scope": "machine-local",
      "reason": "Cloud sessions use Anthropic's own per-session VM isolation. This is a separate, non-equivalent boundary and must not be assumed to carry over."
    },
    "bwrap": {
      "purpose": "bubblewrap; the kernel-namespace primitive srt builds on.",
      "install": "apt-get install -y bubblewrap",
      "scope": "machine-local"
    },
    "socat": {
      "purpose": "Required by claude-safe's policy plumbing.",
      "install": "apt-get install -y socat",
      "scope": "machine-local"
    }
  },
  "relatedRepos": {
    "claude-safe": {
      "localPath": "${HOME}/claude-safe",
      "remote": null,
      "RISK": "Has no git remote as of 2026-09-19. The hardened launcher, its threat model and its six test suites exist on exactly one disk.",
      "installs": [
        "${HOME}/.local/bin/claude-safe",
        "${HOME}/.local/bin/claude-safe-verify"
      ]
    }
  }
}
